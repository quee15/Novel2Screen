export function buildYamlPreviewNotes() {
  return [
    "project/source/characters/story_graph/script/storyboard/review/quality/export",
    "导出前必须执行 Schema 校验",
    "scene_id、character_id、event_id 之间需要保持引用一致"
  ];
}
